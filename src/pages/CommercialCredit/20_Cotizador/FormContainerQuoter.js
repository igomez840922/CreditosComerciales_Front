import React, { useState } from 'react'
import {
    Row,
    Col,
    Container
} from 'react-bootstrap'

const FormContainerQuoter = ({ children }) => {
    return (
        <>
            {children}
        </>
    )
}

export default FormContainerQuoter